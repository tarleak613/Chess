<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .container {
            display: flex;
            align-items: flex-start;
            justify-content: center;
            gap: 20px; /* Space between chessboard and move logs */
            flex-wrap: wrap; /* Allows wrapping on smaller screens */
            max-width: 1200px; /* Limits the maximum width of the container */
            margin: 0 auto;
        }

        .chessboard {
            display: grid;
            grid-template-columns: repeat(8, minmax(0, 1fr));
            grid-template-rows: repeat(8, minmax(0, 1fr));
            width: 100%;
            max-width: 400px;
            aspect-ratio: 1; /* Keeps the chessboard square */
        }

        .piece.white {
            color: white;
            filter: drop-shadow(0 0 2px rgba(0, 0, 0, 1));
        }

        .piece.black {
            color: black;
        }

        .flipped {
            transform: rotate(180deg);
        }

        .square {
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .square.light {
            background-color: #f0d9b5;
        }

        .square.dark {
            background-color: #b58863;
        }

        .piece {
            font-size: 36px;
            cursor: pointer;
        }

        .piece.draggable {
            cursor: grab;
        }

        .dragging {
            opacity: 0.5;
        }

        .flipped .piece {
            transform: rotate(180deg);
        }

        .message {
            margin-bottom: 10px;
            color: white;
            text-align: center;
        }

        .move-log {
            color: white;
            text-align: left;
            max-height: 400px; /* Set max-height to match the chessboard height */
            overflow-y: auto; /* Make the move log scrollable if needed */
            width: 100%;
            max-width: 300px; /* Adjust width as needed */
        }

        .move-log h2 {
            margin-bottom: 10px;
        }

        .move-log ul {
            list-style-type: none;
            padding: 0;
            margin: 0;
        }

        .move-log li {
            margin: 5px 0;
        }

        /* Media Queries */
        @media (max-width: 768px) {
            .container {
                flex-direction: column;
                align-items: center;
            }

            .chessboard {
                max-width: 100%;
                margin: 10px 0;
            }

            .move-log {
                max-width: 100%;
                margin: 10px 0;
            }
        }
    </style>
</head>
<body class="bg-zinc-900 flex flex-col items-center justify-center h-screen">
    <div class="message mb-4 text-white text-lg" id="statusMessage">Waiting for the opponent...</div>
    <div class="container">
        <div class="move-log" id="whiteMovesContainer">
            <h2 class="text-lg text-white px-20">Black's Moves</h2>
            <ul id="whiteMoves"></ul>
        </div>
        <div class="chessboard w-96 h-96"></div>
        <div class="move-log" id="blackMovesContainer">
            <h2 class="text-lg text-white px-20">White's Moves</h2>
            <ul id="blackMoves"></ul>
        </div>
    </div>

    <script src="https://cdn.socket.io/4.7.5/socket.io.min.js" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/chess.js/0.10.3/chess.min.js" crossorigin="anonymous"></script>
    <script src="/js/chessgame.js"></script>
</body>
</html>
